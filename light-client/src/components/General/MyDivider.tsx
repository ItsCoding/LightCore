import { Divider } from "@mui/material"

export const MyDivider = () => {
    return (
        <Divider sx={{
            marginTop: 1,
            // marginBottom: 1,
            borderColor: "rgba(255, 255, 255, 0.12)"
        }} />
    )
}